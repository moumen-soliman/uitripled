a:I[996454,["/_next/static/chunks/dc92709d96e51da4.js","/_next/static/chunks/28d31646f911358e.js","/_next/static/chunks/b0689fc5070200a0.js","/_next/static/chunks/d55ca8c48b5d5d51.js","/_next/static/chunks/2363ff9e745ac33a.js","/_next/static/chunks/5d9ce5f95154af4a.js","/_next/static/chunks/fe35a3e987c156fc.js","/_next/static/chunks/b794c6d381badd61.js","/_next/static/chunks/f7bfd565bd2f913f.js","/_next/static/chunks/b59d4e65b6fc885c.js","/_next/static/chunks/becf1da93e15457b.js","/_next/static/chunks/fdd25d98951f5402.js","/_next/static/chunks/91ff2107e32627dd.js","/_next/static/chunks/0d9443edae03239b.js","/_next/static/chunks/caa50e77cf52fdaa.js","/_next/static/chunks/e0bc9c3a4105a6fe.js","/_next/static/chunks/48913bc585f72fa4.js"],"NativeLiquidButtonSuccess"]
b:I[996454,["/_next/static/chunks/dc92709d96e51da4.js","/_next/static/chunks/28d31646f911358e.js","/_next/static/chunks/b0689fc5070200a0.js","/_next/static/chunks/d55ca8c48b5d5d51.js","/_next/static/chunks/2363ff9e745ac33a.js","/_next/static/chunks/5d9ce5f95154af4a.js","/_next/static/chunks/fe35a3e987c156fc.js","/_next/static/chunks/b794c6d381badd61.js","/_next/static/chunks/f7bfd565bd2f913f.js","/_next/static/chunks/b59d4e65b6fc885c.js","/_next/static/chunks/becf1da93e15457b.js","/_next/static/chunks/fdd25d98951f5402.js","/_next/static/chunks/91ff2107e32627dd.js","/_next/static/chunks/0d9443edae03239b.js","/_next/static/chunks/caa50e77cf52fdaa.js","/_next/static/chunks/e0bc9c3a4105a6fe.js","/_next/static/chunks/48913bc585f72fa4.js"],"NativeLiquidButtonError"]
14:I[897367,["/_next/static/chunks/d96012bcfc98706a.js","/_next/static/chunks/c1216787d4b31b65.js"],"OutletBoundary"]
15:"$Sreact.suspense"
3:T1b66,"use client";

import {
  NativeButton,
  type NativeButtonProps,
} from "@/components/native/shadcn-ui/native-button-shadcnui";
import { cn } from "@/lib/utils";
import { AnimatePresence, motion } from "framer-motion";
import { Loader2 } from "lucide-react";
import { useEffect, useState } from "react";

export interface NativeLiquidButtonProps
  extends Omit<NativeButtonProps, "onClick" | "loading"> {
  /**
   * Progress value (0-100)
   */
  progress?: number;
  /**
   * Loading state
   */
  loading?: boolean;
  /**
   * Callback when button is clicked
   */
  onClick?: () => void | Promise<void>;
  /**
   * Visual variant
   * Default: "default"
   */
  liquidVariant?: "default" | "gradient" | "glow" | "wave";
  /**
   * Liquid color
   */
  liquidColor?: string;
  /**
   * Show percentage text
   * Default: false
   */
  showPercentage?: boolean;
  /**
   * Auto-simulate loading (for demo purposes)
   */
  autoSimulate?: boolean;
  /**
   * Success state
   */
  success?: boolean;
  /**
   * Error state
   */
  error?: boolean;
}

const sizeVariants = {
  sm: "h-9 px-4 text-sm min-w-[100px]",
  default: "h-11 px-6 text-base min-w-[140px]",
  lg: "h-14 px-8 text-lg min-w-[180px]",
  icon: "h-11 px-6 text-base min-w-[140px]",
};

export function NativeLiquidButton({
  children,
  className,
  variant = "default",
  size = "default",
  progress = 0,
  loading = false,
  onClick,
  disabled,
  liquidVariant = "default",
  liquidColor,
  showPercentage = false,
  autoSimulate = false,
  success = false,
  error = false,
  ...props
}: NativeLiquidButtonProps) {
  const [internalProgress, setInternalProgress] = useState(progress);
  const [isSimulating, setIsSimulating] = useState(false);

  useEffect(() => {
    setInternalProgress(progress);
  }, [progress]);

  useEffect(() => {
    if (autoSimulate && isSimulating) {
      const interval = setInterval(() => {
        setInternalProgress((prev) => {
          if (prev >= 100) {
            clearInterval(interval);
            setIsSimulating(false);
            return 100;
          }
          return prev + Math.random() * 10;
        });
      }, 200);
      return () => clearInterval(interval);
    }
  }, [autoSimulate, isSimulating]);

  const handleClick = async () => {
    if (disabled || loading) return;

    if (autoSimulate) {
      setIsSimulating(true);
      setInternalProgress(0);
    }

    if (onClick) {
      await onClick();
    }
  };

  const getLiquidColor = () => {
    if (success) return "bg-green-500";
    if (error) return "bg-red-500";
    if (liquidColor) return liquidColor;

    switch (liquidVariant) {
      case "gradient":
        return "bg-gradient-to-r from-primary via-primary/80 to-primary";
      case "glow":
        return "bg-primary";
      default:
        return "bg-primary";
    }
  };

  const clampedProgress = Math.min(Math.max(internalProgress, 0), 100);

  return (
    <div className="relative inline-block">
      <NativeButton
        variant={variant}
        size={size}
        loading={false}
        disabled={disabled || loading}
        onClick={handleClick}
        className={cn(
          sizeVariants[size || "default"],
          "relative overflow-hidden font-semibold transition-all duration-300",
          "before:absolute before:inset-0 before:bg-background/20 before:pointer-events-none before:rounded-md",
          liquidVariant === "glow" &&
            !disabled &&
            "shadow-lg shadow-primary/30",
          className
        )}
        {...props}
      >
        {/* Liquid fill effect */}
        <motion.div
          className={cn("absolute inset-0 origin-left", getLiquidColor())}
          initial={{ scaleX: 0 }}
          animate={{ scaleX: clampedProgress / 100 }}
          transition={{
            type: "spring",
            stiffness: 100,
            damping: 20,
          }}
        >
          {/* Bubble effects */}
          {liquidVariant === "default" && clampedProgress > 0 && (
            <>
              {[...Array(3)].map((_, i) => (
                <motion.div
                  key={i}
                  className="absolute bottom-0 w-2 h-2 bg-white/30 rounded-md"
                  style={{
                    left: `${20 + i * 25}%`,
                  }}
                  animate={{
                    y: [-10, -50, -10],
                    opacity: [0, 1, 0],
                    scale: [0.5, 1, 0.5],
                  }}
                  transition={{
                    duration: 2,
                    repeat: Number.POSITIVE_INFINITY,
                    delay: i * 0.4,
                    ease: "easeInOut",
                  }}
                />
              ))}
            </>
          )}

          {/* Shimmer effect for gradient */}
          {liquidVariant === "gradient" && clampedProgress > 0 && (
            <motion.div
              className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent"
              animate={{
                x: ["-100%", "200%"],
              }}
              transition={{
                duration: 1.5,
                repeat: Number.POSITIVE_INFINITY,
                ease: "linear",
              }}
            />
          )}
        </motion.div>

        {/* Button content */}
        <span className="relative z-10 flex items-center justify-center gap-2">
          <AnimatePresence mode="wait">
            {loading && (
              <motion.div
                key="loading"
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                exit={{ opacity: 0 }}
              >
                <Loader2 className="w-4 h-4 animate-spin" />
              </motion.div>
            )}
          </AnimatePresence>

          <motion.span
            animate={loading ? { opacity: [1, 0.5, 1] } : { opacity: 1 }}
            transition={
              loading
                ? {
                    duration: 1.5,
                    repeat: Number.POSITIVE_INFINITY,
                    ease: "easeInOut",
                  }
                : { duration: 0.2 }
            }
          >
            {children}
          </motion.span>

          {showPercentage && (
            <motion.span
              key={clampedProgress}
              initial={{ scale: 1.2, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              className="text-xs font-mono"
            >
              {Math.round(clampedProgress)}%
            </motion.span>
          )}
        </span>

        {/* Glow effect */}
        {liquidVariant === "glow" && !disabled && clampedProgress > 0 && (
          <motion.div
            className="absolute inset-0 bg-primary/20 blur-xl"
            animate={{
              opacity: [0.3, 0.6, 0.3],
            }}
            transition={{
              duration: 2,
              repeat: Number.POSITIVE_INFINITY,
              ease: "easeInOut",
            }}
          />
        )}
      </NativeButton>
    </div>
  );
}
c:T1b49,"use client";

import { cn } from "@/lib/utils";
import { AnimatePresence, motion } from "framer-motion";
import { Loader2 } from "lucide-react";
import { useEffect, useState } from "react";
import { NativeButton, type NativeButtonProps } from "./native-button-baseui";

export interface NativeLiquidButtonProps
  extends Omit<NativeButtonProps, "onClick" | "loading"> {
  /**
   * Progress value (0-100)
   */
  progress?: number;
  /**
   * Loading state
   */
  loading?: boolean;
  /**
   * Callback when button is clicked
   */
  onClick?: () => void | Promise<void>;
  /**
   * Visual variant
   * Default: "default"
   */
  liquidVariant?: "default" | "gradient" | "glow" | "wave";
  /**
   * Liquid color
   */
  liquidColor?: string;
  /**
   * Show percentage text
   * Default: false
   */
  showPercentage?: boolean;
  /**
  /**
   * Auto-simulate loading (for demo purposes)
   */
  autoSimulate?: boolean;
  /**
   * Success state
   */
  success?: boolean;
  /**
   * Error state
   */
  error?: boolean;
}

const sizeVariants = {
  sm: "h-9 px-4 text-sm min-w-[100px]",
  default: "h-11 px-6 text-base min-w-[140px]",
  lg: "h-14 px-8 text-lg min-w-[180px]",
  icon: "h-11 px-6 text-base min-w-[140px]",
};

export function NativeLiquidButton({
  children,
  className,
  variant = "default",
  size = "default",
  progress = 0,
  loading = false,
  onClick,
  disabled,
  liquidVariant = "default",
  liquidColor,
  showPercentage = false,
  autoSimulate = false,
  success = false,
  error = false,
  ...props
}: NativeLiquidButtonProps) {
  const [internalProgress, setInternalProgress] = useState(progress);
  const [isSimulating, setIsSimulating] = useState(false);

  useEffect(() => {
    setInternalProgress(progress);
  }, [progress]);

  useEffect(() => {
    if (autoSimulate && isSimulating) {
      const interval = setInterval(() => {
        setInternalProgress((prev) => {
          if (prev >= 100) {
            clearInterval(interval);
            setIsSimulating(false);
            return 100;
          }
          return prev + Math.random() * 10;
        });
      }, 200);
      return () => clearInterval(interval);
    }
  }, [autoSimulate, isSimulating]);

  const handleClick = async () => {
    if (disabled || loading) return;

    if (autoSimulate) {
      setIsSimulating(true);
      setInternalProgress(0);
    }

    if (onClick) {
      await onClick();
    }
  };

  const getLiquidColor = () => {
    if (success) return "bg-green-500";
    if (error) return "bg-red-500";
    if (liquidColor) return liquidColor;

    switch (liquidVariant) {
      case "gradient":
        return "bg-gradient-to-r from-primary via-primary/80 to-primary";
      case "glow":
        return "bg-primary";
      default:
        return "bg-primary";
    }
  };

  const clampedProgress = Math.min(Math.max(internalProgress, 0), 100);

  return (
    <div className="relative inline-block">
      <NativeButton
        variant={variant}
        size={size}
        loading={false}
        disabled={disabled || loading}
        onClick={handleClick}
        className={cn(
          sizeVariants[size || "default"],
          "relative overflow-hidden font-semibold transition-all duration-300",
          "before:absolute before:inset-0 before:bg-background/20 before:pointer-events-none before:rounded-md",
          liquidVariant === "glow" &&
            !disabled &&
            "shadow-lg shadow-primary/30",
          className
        )}
        {...props}
      >
        {/* Liquid fill effect */}
        <motion.div
          className={cn("absolute inset-0 origin-left", getLiquidColor())}
          initial={{ scaleX: 0 }}
          animate={{ scaleX: clampedProgress / 100 }}
          transition={{
            type: "spring",
            stiffness: 100,
            damping: 20,
          }}
        >
          {/* Bubble effects */}
          {liquidVariant === "default" && clampedProgress > 0 && (
            <>
              {[...Array(3)].map((_, i) => (
                <motion.div
                  key={i}
                  className="absolute bottom-0 w-2 h-2 bg-white/30 rounded-md"
                  style={{
                    left: `${20 + i * 25}%`,
                  }}
                  animate={{
                    y: [-10, -50, -10],
                    opacity: [0, 1, 0],
                    scale: [0.5, 1, 0.5],
                  }}
                  transition={{
                    duration: 2,
                    repeat: Number.POSITIVE_INFINITY,
                    delay: i * 0.4,
                    ease: "easeInOut",
                  }}
                />
              ))}
            </>
          )}

          {/* Shimmer effect for gradient */}
          {liquidVariant === "gradient" && clampedProgress > 0 && (
            <motion.div
              className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent"
              animate={{
                x: ["-100%", "200%"],
              }}
              transition={{
                duration: 1.5,
                repeat: Number.POSITIVE_INFINITY,
                ease: "linear",
              }}
            />
          )}
        </motion.div>

        {/* Button content */}
        <span className="relative z-10 flex items-center justify-center gap-2">
          <AnimatePresence mode="wait">
            {loading && (
              <motion.div
                key="loading"
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                exit={{ opacity: 0 }}
              >
                <Loader2 className="w-4 h-4 animate-spin" />
              </motion.div>
            )}
          </AnimatePresence>

          <motion.span
            animate={loading ? { opacity: [1, 0.5, 1] } : { opacity: 1 }}
            transition={
              loading
                ? {
                    duration: 1.5,
                    repeat: Number.POSITIVE_INFINITY,
                    ease: "easeInOut",
                  }
                : { duration: 0.2 }
            }
          >
            {children}
          </motion.span>

          {showPercentage && (
            <motion.span
              key={clampedProgress}
              initial={{ scale: 1.2, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              className="text-xs font-mono"
            >
              {Math.round(clampedProgress)}%
            </motion.span>
          )}
        </span>

        {/* Glow effect */}
        {liquidVariant === "glow" && !disabled && clampedProgress > 0 && (
          <motion.div
            className="absolute inset-0 bg-primary/20 blur-xl"
            animate={{
              opacity: [0.3, 0.6, 0.3],
            }}
            transition={{
              duration: 2,
              repeat: Number.POSITIVE_INFINITY,
              ease: "easeInOut",
            }}
          />
        )}
      </NativeButton>
    </div>
  );
}
d:T1b66,"use client";

import {
  NativeButton,
  type NativeButtonProps,
} from "@/components/native/shadcn-ui/native-button-shadcnui";
import { cn } from "@/lib/utils";
import { AnimatePresence, motion } from "framer-motion";
import { Loader2 } from "lucide-react";
import { useEffect, useState } from "react";

export interface NativeLiquidButtonProps
  extends Omit<NativeButtonProps, "onClick" | "loading"> {
  /**
   * Progress value (0-100)
   */
  progress?: number;
  /**
   * Loading state
   */
  loading?: boolean;
  /**
   * Callback when button is clicked
   */
  onClick?: () => void | Promise<void>;
  /**
   * Visual variant
   * Default: "default"
   */
  liquidVariant?: "default" | "gradient" | "glow" | "wave";
  /**
   * Liquid color
   */
  liquidColor?: string;
  /**
   * Show percentage text
   * Default: false
   */
  showPercentage?: boolean;
  /**
   * Auto-simulate loading (for demo purposes)
   */
  autoSimulate?: boolean;
  /**
   * Success state
   */
  success?: boolean;
  /**
   * Error state
   */
  error?: boolean;
}

const sizeVariants = {
  sm: "h-9 px-4 text-sm min-w-[100px]",
  default: "h-11 px-6 text-base min-w-[140px]",
  lg: "h-14 px-8 text-lg min-w-[180px]",
  icon: "h-11 px-6 text-base min-w-[140px]",
};

export function NativeLiquidButton({
  children,
  className,
  variant = "default",
  size = "default",
  progress = 0,
  loading = false,
  onClick,
  disabled,
  liquidVariant = "default",
  liquidColor,
  showPercentage = false,
  autoSimulate = false,
  success = false,
  error = false,
  ...props
}: NativeLiquidButtonProps) {
  const [internalProgress, setInternalProgress] = useState(progress);
  const [isSimulating, setIsSimulating] = useState(false);

  useEffect(() => {
    setInternalProgress(progress);
  }, [progress]);

  useEffect(() => {
    if (autoSimulate && isSimulating) {
      const interval = setInterval(() => {
        setInternalProgress((prev) => {
          if (prev >= 100) {
            clearInterval(interval);
            setIsSimulating(false);
            return 100;
          }
          return prev + Math.random() * 10;
        });
      }, 200);
      return () => clearInterval(interval);
    }
  }, [autoSimulate, isSimulating]);

  const handleClick = async () => {
    if (disabled || loading) return;

    if (autoSimulate) {
      setIsSimulating(true);
      setInternalProgress(0);
    }

    if (onClick) {
      await onClick();
    }
  };

  const getLiquidColor = () => {
    if (success) return "bg-green-500";
    if (error) return "bg-red-500";
    if (liquidColor) return liquidColor;

    switch (liquidVariant) {
      case "gradient":
        return "bg-gradient-to-r from-primary via-primary/80 to-primary";
      case "glow":
        return "bg-primary";
      default:
        return "bg-primary";
    }
  };

  const clampedProgress = Math.min(Math.max(internalProgress, 0), 100);

  return (
    <div className="relative inline-block">
      <NativeButton
        variant={variant}
        size={size}
        loading={false}
        disabled={disabled || loading}
        onClick={handleClick}
        className={cn(
          sizeVariants[size || "default"],
          "relative overflow-hidden font-semibold transition-all duration-300",
          "before:absolute before:inset-0 before:bg-background/20 before:pointer-events-none before:rounded-md",
          liquidVariant === "glow" &&
            !disabled &&
            "shadow-lg shadow-primary/30",
          className
        )}
        {...props}
      >
        {/* Liquid fill effect */}
        <motion.div
          className={cn("absolute inset-0 origin-left", getLiquidColor())}
          initial={{ scaleX: 0 }}
          animate={{ scaleX: clampedProgress / 100 }}
          transition={{
            type: "spring",
            stiffness: 100,
            damping: 20,
          }}
        >
          {/* Bubble effects */}
          {liquidVariant === "default" && clampedProgress > 0 && (
            <>
              {[...Array(3)].map((_, i) => (
                <motion.div
                  key={i}
                  className="absolute bottom-0 w-2 h-2 bg-white/30 rounded-md"
                  style={{
                    left: `${20 + i * 25}%`,
                  }}
                  animate={{
                    y: [-10, -50, -10],
                    opacity: [0, 1, 0],
                    scale: [0.5, 1, 0.5],
                  }}
                  transition={{
                    duration: 2,
                    repeat: Number.POSITIVE_INFINITY,
                    delay: i * 0.4,
                    ease: "easeInOut",
                  }}
                />
              ))}
            </>
          )}

          {/* Shimmer effect for gradient */}
          {liquidVariant === "gradient" && clampedProgress > 0 && (
            <motion.div
              className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent"
              animate={{
                x: ["-100%", "200%"],
              }}
              transition={{
                duration: 1.5,
                repeat: Number.POSITIVE_INFINITY,
                ease: "linear",
              }}
            />
          )}
        </motion.div>

        {/* Button content */}
        <span className="relative z-10 flex items-center justify-center gap-2">
          <AnimatePresence mode="wait">
            {loading && (
              <motion.div
                key="loading"
                initial={{ opacity: 0 }}
                animate={{ opacity: 1 }}
                exit={{ opacity: 0 }}
              >
                <Loader2 className="w-4 h-4 animate-spin" />
              </motion.div>
            )}
          </AnimatePresence>

          <motion.span
            animate={loading ? { opacity: [1, 0.5, 1] } : { opacity: 1 }}
            transition={
              loading
                ? {
                    duration: 1.5,
                    repeat: Number.POSITIVE_INFINITY,
                    ease: "easeInOut",
                  }
                : { duration: 0.2 }
            }
          >
            {children}
          </motion.span>

          {showPercentage && (
            <motion.span
              key={clampedProgress}
              initial={{ scale: 1.2, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              className="text-xs font-mono"
            >
              {Math.round(clampedProgress)}%
            </motion.span>
          )}
        </span>

        {/* Glow effect */}
        {liquidVariant === "glow" && !disabled && clampedProgress > 0 && (
          <motion.div
            className="absolute inset-0 bg-primary/20 blur-xl"
            animate={{
              opacity: [0.3, 0.6, 0.3],
            }}
            transition={{
              duration: 2,
              repeat: Number.POSITIVE_INFINITY,
              ease: "easeInOut",
            }}
          />
        )}
      </NativeButton>
    </div>
  );
}
e:Tf32,"use client";

import { NativeLiquidButton } from "@/components/native/base-ui/native-liquid-button-baseui";
import { useState } from "react";

export function NativeLiquidButtonDefault() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton autoSimulate>Submit</NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonGradient() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="gradient" autoSimulate>
        Download
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonGlow() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="glow" autoSimulate>
        Complete
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonWave() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="wave" autoSimulate>
        Process
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonWithProgress() {
  const [progress, setProgress] = useState(0);

  const handleClick = async () => {
    // Simulate progress
    for (let i = 0; i <= 100; i += 10) {
      await new Promise((resolve) => setTimeout(resolve, 200));
      setProgress(i);
    }
  };

  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton
        progress={progress}
        onClick={handleClick}
        showPercentage
      >
        Upload File
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonLoading() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton loading progress={50}>
        Processing...
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonSuccess() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton success progress={100}>
        Success!
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonError() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton error progress={100}>
        Failed
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonSizes() {
  return (
    <div className="flex items-center justify-center gap-4 p-8 min-h-[200px] flex-wrap">
      <NativeLiquidButton size="sm" autoSimulate>
        Small
      </NativeLiquidButton>
      <NativeLiquidButton size="default" autoSimulate>
        Medium
      </NativeLiquidButton>
      <NativeLiquidButton size="lg" autoSimulate>
        Large
      </NativeLiquidButton>
      <NativeLiquidButton size="lg" autoSimulate>
        Extra Large
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonVariants() {
  return (
    <div className="flex items-center justify-center gap-4 p-8 min-h-[200px] flex-wrap">
      <NativeLiquidButton variant="default" autoSimulate>
        Default
      </NativeLiquidButton>
      <NativeLiquidButton variant="outline" autoSimulate>
        Outline
      </NativeLiquidButton>
      <NativeLiquidButton variant="secondary" autoSimulate>
        Secondary
      </NativeLiquidButton>
      <NativeLiquidButton variant="ghost" autoSimulate>
        Ghost
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonCustomColor() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidColor="bg-purple-500" autoSimulate>
        Custom Color
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonDemo() {
  return <NativeLiquidButtonDefault />;
}
f:Tf36,"use client";

import { NativeLiquidButton } from "@/components/native/shadcn-ui/native-liquid-button-shadcnui";
import { useState } from "react";

export function NativeLiquidButtonDefault() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton autoSimulate>Submit</NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonGradient() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="gradient" autoSimulate>
        Download
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonGlow() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="glow" autoSimulate>
        Complete
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonWave() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidVariant="wave" autoSimulate>
        Process
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonWithProgress() {
  const [progress, setProgress] = useState(0);

  const handleClick = async () => {
    // Simulate progress
    for (let i = 0; i <= 100; i += 10) {
      await new Promise((resolve) => setTimeout(resolve, 200));
      setProgress(i);
    }
  };

  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton
        progress={progress}
        onClick={handleClick}
        showPercentage
      >
        Upload File
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonLoading() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton loading progress={50}>
        Processing...
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonSuccess() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton success progress={100}>
        Success!
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonError() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton error progress={100}>
        Failed
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonSizes() {
  return (
    <div className="flex items-center justify-center gap-4 p-8 min-h-[200px] flex-wrap">
      <NativeLiquidButton size="sm" autoSimulate>
        Small
      </NativeLiquidButton>
      <NativeLiquidButton size="default" autoSimulate>
        Medium
      </NativeLiquidButton>
      <NativeLiquidButton size="lg" autoSimulate>
        Large
      </NativeLiquidButton>
      <NativeLiquidButton size="lg" autoSimulate>
        Extra Large
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonVariants() {
  return (
    <div className="flex items-center justify-center gap-4 p-8 min-h-[200px] flex-wrap">
      <NativeLiquidButton variant="default" autoSimulate>
        Default
      </NativeLiquidButton>
      <NativeLiquidButton variant="outline" autoSimulate>
        Outline
      </NativeLiquidButton>
      <NativeLiquidButton variant="secondary" autoSimulate>
        Secondary
      </NativeLiquidButton>
      <NativeLiquidButton variant="ghost" autoSimulate>
        Ghost
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonCustomColor() {
  return (
    <div className="flex items-center justify-center p-8 min-h-[200px]">
      <NativeLiquidButton liquidColor="bg-purple-500" autoSimulate>
        Custom Color
      </NativeLiquidButton>
    </div>
  );
}

export function NativeLiquidButtonDemo() {
  return <NativeLiquidButtonDefault />;
}
0:{"buildId":"bFBIq5AO28jMw9-shWxSc","rsc":["$","$1","c",{"children":[["$","$L2",null,{"code":"$3","relatedComponents":[{"id":"default","name":"Default","description":"Standard liquid button with bubble effects","component":"$4","code":"<NativeLiquidButton autoSimulate>\n  Submit\n</NativeLiquidButton>"},{"id":"gradient","name":"Gradient","description":"Liquid button with gradient fill and shimmer effect","component":"$5","code":"<NativeLiquidButton liquidVariant=\"gradient\" autoSimulate>\n  Download\n</NativeLiquidButton>"},{"id":"glow","name":"Glow","description":"Liquid button with glowing effect","component":"$6","code":"<NativeLiquidButton liquidVariant=\"glow\" autoSimulate>\n  Complete\n</NativeLiquidButton>"},{"id":"wave","name":"Wave","description":"Liquid button with animated wave pattern","component":"$7","code":"<NativeLiquidButton liquidVariant=\"wave\" autoSimulate>\n  Process\n</NativeLiquidButton>"},{"id":"with-progress","name":"With Progress","description":"Button with manual progress tracking and percentage display","component":"$8","code":"<NativeLiquidButton\n  progress={progress}\n  onClick={handleClick}\n  showPercentage\n>\n  Upload File\n</NativeLiquidButton>"},{"id":"loading","name":"Loading State","description":"Button in loading state with spinner","component":"$9","code":"<NativeLiquidButton loading progress={50}>\n  Processing...\n</NativeLiquidButton>"},{"id":"success","name":"Success State","description":"Button showing success state with checkmark","component":"$a","code":"<NativeLiquidButton success progress={100}>\n  Success!\n</NativeLiquidButton>"},{"id":"error","name":"Error State","description":"Button showing error state with X icon","component":"$b","code":"<NativeLiquidButton error progress={100}>\n  Failed\n</NativeLiquidButton>"}],"variantCodes":{"default":"<NativeLiquidButton autoSimulate>\n  Submit\n</NativeLiquidButton>","gradient":"<NativeLiquidButton liquidVariant=\"gradient\" autoSimulate>\n  Download\n</NativeLiquidButton>","glow":"<NativeLiquidButton liquidVariant=\"glow\" autoSimulate>\n  Complete\n</NativeLiquidButton>","wave":"<NativeLiquidButton liquidVariant=\"wave\" autoSimulate>\n  Process\n</NativeLiquidButton>","with-progress":"<NativeLiquidButton\n  progress={progress}\n  onClick={handleClick}\n  showPercentage\n>\n  Upload File\n</NativeLiquidButton>","loading":"<NativeLiquidButton loading progress={50}>\n  Processing...\n</NativeLiquidButton>","success":"<NativeLiquidButton success progress={100}>\n  Success!\n</NativeLiquidButton>","error":"<NativeLiquidButton error progress={100}>\n  Failed\n</NativeLiquidButton>"},"baseId":"native-liquid-button","baseuiCode":"$c","shadcnuiCode":"$d","baseuiDemoCode":"$e","shadcnuiDemoCode":"$f"}],["$L10","$L11","$L12"],"$L13"]}],"loading":null,"isPartial":false}
10:["$","script","script-0",{"src":"/_next/static/chunks/caa50e77cf52fdaa.js","async":true}]
11:["$","script","script-1",{"src":"/_next/static/chunks/e0bc9c3a4105a6fe.js","async":true}]
12:["$","script","script-2",{"src":"/_next/static/chunks/48913bc585f72fa4.js","async":true}]
13:["$","$L14",null,{"children":["$","$15",null,{"name":"Next.MetadataOutlet","children":"$@16"}]}]
16:null
